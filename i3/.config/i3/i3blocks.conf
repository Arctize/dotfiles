separator_block_width=48
command=/usr/*/i3blocks/$BLOCK_NAME
markup=pango

[mpd]
command=mpc current
interval=2

[root]
command=df -h / | tail -n 1 | awk '{print $6": "$3"/"$2" ("$5")"}'
interval=30

[home]
command=df -h /home | tail -n 1 | awk '{print $6": "$3"/"$2" ("$5")"}'
interval=30

[temp]
command=sensors | grep Package | awk '{print substr($4, 2, 10)}'
interval=3

[memory]
command=free -h | grep Mem | awk '{print $3"/"$2}'
interval=3

[network]
command=ip route | tail -n 1 | awk '{print $3 ": "$9}'
interval=5

[brightness]
command=xbacklight -get 2>/dev/null | sed 's/\..*//'
interval=3

[battery]
#command=acpi -b | awk '{print $3" "$4" "$5}'
command=~/.config/i3/scripts/battery.sh
interval=1

[volume]
instance=Master
interval=15
signal=1

[date]
command=date '+%A, %b %d, %H:%M:%S'
interval=1
